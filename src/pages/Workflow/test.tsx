import React from 'react';

// This is a simple test component to verify that the module can be imported correctly
export default function TestComponent() {
  return (
    <div>
      <h1>Test Component</h1>
      <p>This is a test component to verify that the module can be imported correctly.</p>
    </div>
  );
} 