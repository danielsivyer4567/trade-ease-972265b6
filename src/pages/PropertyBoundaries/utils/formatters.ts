
/**
 * Format a coordinate value to a readable format
 * @param value The coordinate value to format
 * @returns Formatted coordinate string
 */
export const formatCoordinate = (value: number): string => {
  return value.toFixed(6);
};
